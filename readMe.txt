# OS_GroupA_Project_02
In order to this program it needs to be compiled as:
gcc driver.c manager.c assisant.c historySearch.c -o driver
and as
gcc server.c -o server
for the server.

The program can then run by running the server executable first then the driver
